import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const password = 'test'; await prisma.user.createMany({ data: [{ name: 'Test', firstName: 'T', lastName: 'T', email: 'test@gmail.com', password, type: 'ADMIN' as any, city: 'Dhaka', bio: 'Sys', emailVerifiedAt: new Date() }] }).catch(e => console.log('ERROR IS:', e.message)); } main();
